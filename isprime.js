function primes(start,end)

{
  const prime=numuber=>{
    
    for (let i=2;i<number;i++)
      {
        if (number%i===0)
          return null;
        
      }

    return number !== 1;
    
  };
  return primes.filter(start,end);

  

}
